# Future English Version

This folder is reserved for English-language source planning.

Suggested structure when translation begins:

- `src/content/pages-en/` or a configured `pagesEn` collection
- `src/content/volumes-en/` or a configured `volumesEn` collection
- Locale-aware route prefixes such as `/en/`
- Shared chapter identifiers between Chinese and English Markdown files

The current Astro content configuration intentionally excludes this folder so the Chinese site remains stable until English content is ready.
